# Vite loads these when the desktop/renderer dev server starts.
# Copy to `.env` in this directory.

# JARVIS FastAPI base URL (no trailing slash)
VITE_API_BASE_URL=http://127.0.0.1:8000

# Default Ollama model label shown in UI / sent to API where applicable
VITE_OLLAMA_MODEL=llama3
